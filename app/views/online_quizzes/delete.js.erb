var x= <%= rem %>
var from=<%= from_lec %>

// remove partial only if i'm deleting the quiz im editing.
if($("a.edit.active").attr("id")==""+x)
{
	//$('#videospan').html("") // won't remove partial.. only remove quiz.
	$('.edit').attr("disabled",false)
	document.getElementById("ontop").innerHTML="" // empty questions.
	$('.ontop').css("min-height","278px");
	$('.ontop').unbind();
	
}

$(".fields#online_quiz_"+x).remove();

